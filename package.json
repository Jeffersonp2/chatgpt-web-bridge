{
  "name": "testegpt-local-bridge",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Local OpenAI-compatible bridge backed by a user-authenticated ChatGPT Web session.",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "install:browser": "playwright install chromium",
    "check": "node --check src/server.js && node --check src/browser.js && node --check src/remote-login.js && node --check src/remote-attachment.js && node --check scripts/build-n8n-workflow.mjs && node --test",
    "install:remote-login": "bash scripts/install-remote-login.sh"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^5.1.0",
    "playwright": "^1.55.0",
    "multer": "^2.0.2",
    "@novnc/novnc": "^1.6.0",
    "ws": "^8.18.3",
    "dotenv": "^16.4.5"
  }
}
